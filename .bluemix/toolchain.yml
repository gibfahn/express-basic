---
# TODO(gib): Work out whether we even need these files
# This is currently just sample code from other apps, we probably won't need
# all or any of it.
name: "app toolchain"
description: "Add tools to this toolchain as needed."
version: 1.0
required:
  # TODO(gib): what do we actually need?
  - deploy
  - openwhisk
  - coveralls

# Github repos
app-repo:
  service_id: githubpublic
  parameters:
    repo_name: <%= appname %>
    repo_url: "https://github.com/<%= username %>/<%= appname %>.git"
